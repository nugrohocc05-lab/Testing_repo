if ($request->query('debug_color_mismatch')) {
    dd([
        'outputPerIdpoColor_total' => $outputPerIdpoColor->sum(),
        'poList_output_total' => $poList->sum(fn ($item) => $outputPerIdpoColor->get($item->idpo.'|'.$item->idcolor, 0)),
        'idcolor_keys_in_output' => $outputPerIdpoColor->keys()->values()->toArray(),
        'idcolor_keys_in_poList' => $poList->map(fn ($i) => $i->idpo.'|'.$i->idcolor)->values()->toArray(),
    ]);
}