// SEBELUM
$originalNotFinalIdpoList = $split['overdue']
    ->merge($split['need_final'])
    ->unique()
    ->diff($status['finalCom'])
    ->values();

// SESUDAH
$activeNotFinalCandidate = $this->getActiveRerouteIdpoList(
    $split['overdue']->merge($split['need_final'])->unique()->values()
);

$remappedFinalCom = $this->remapToReroutedIdpo($status['finalCom']);

$originalNotFinalIdpoList = $activeNotFinalCandidate
    ->diff($remappedFinalCom)
    ->values();