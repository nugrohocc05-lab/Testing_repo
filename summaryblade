DELIMITER $$

CREATE TRIGGER trg_sewing_outputs_cap_updated
BEFORE UPDATE ON sewing_outputs
FOR EACH ROW
BEGIN
    IF NOT (OLD.a <=> NEW.a AND OLD.b <=> NEW.b AND OLD.c <=> NEW.c AND OLD.d <=> NEW.d) THEN
        SET NEW.cap_updated_at = NOW();
    END IF;
END$$

DELIMITER ;