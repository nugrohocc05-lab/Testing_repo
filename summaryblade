public function storeDefect(Request $request) // ganti sesuai nama function aslinya
{
    dd([
        'request_all'       => $request->all(),
        'inline_header_id'  => $request->inline_header_id, // atau nama field yang dipakai
        'header_data'       => \App\Http\Models\InlineHeader::find($request->inline_header_id),
    ]);

    // ... kode asli di bawah ini
}