graphic: [
    {
        type: 'text',
        left: 'center',
        top: 'center',
        style: {
            text: '{value|' + total.toLocaleString('id-ID') + valueSuffix + '}\n{label|' + centerLabel + '}',
            rich: {
                value: {
                    fontSize: center,
                    fontWeight: 'bold',
                    fill: '#ffffff',
                    lineHeight: Math.round(center * 1.25),
                    align: 'center'
                },
                label: {
                    fontSize: sub,
                    fontWeight: 'normal',
                    fill: '#ffffff',
                    lineHeight: Math.round(sub * 1.4),
                    align: 'center'
                }
            }
        }
    }
]

@media (min-width: 992px) and (max-width: 1400px) {
    .chart-donut {
        min-height: 130px;
    }
    /* ...rule lain yang sudah ada... */
}