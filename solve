@extends('layouts.masternonavbar')

@section('extracss')
<style>
    .stat-card {
        height: 160px !important;
        min-height: 160px !important;
    }

    .stat-card img {
        width: 40px !important;
        height: 40px !important;
        margin-bottom: .45rem !important;
    }

    .stat-card h6 {
        font-size: 1rem;
        margin-bottom: .25rem !important;
    }

    .stat-card h2 {
        font-size: 1.5rem;
        line-height: 1;
    }

    .stat-card small {
        font-size: 1rem;
    }

    .stat-card-defect {
        height: auto;
    }

    .stat-card-defect > div {
        width: 100%;
    }

    .defect-header {
        width: 100%;
    }

    .defect-list {
        width: 100%;
        font-size: 13px;
        gap: 2px;
    }

    #topDefectList {
        max-height: 110px;
        width: 100%;
        overflow-y: auto;
        overflow-x: hidden;
        -webkit-overflow-scrolling: touch;
    }

    #topDefectList::-webkit-scrollbar {
        width: 3px;
    }

    #topDefectList::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.3);
        border-radius: 4px;
    }

    #topDefectList .defect-item {
        display: flex;
        align-items: center;
        width: 100%;
        margin-bottom: 1px;
    }

    #topDefectList .defect-item:last-child {
        margin-bottom: 0;
    }

    .defect-circle {
        width: 12px;
        height: 12px;
    }

    .defect-name {
        flex: 1;
        min-width: 0;
        margin-left: 8px;
        text-align: left;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .defect-total {
        flex: 0 0 auto;
        margin-left: 8px;
        white-space: nowrap;
    }

    .defect-percent {
        flex: 0 0 55px;
        margin-left: 8px;
        text-align: right;
        white-space: nowrap;
    }

    .icon-summary {
        width: 25px;
        height: 25px;
    }

    .dashboard-header {
        width: 100%;
    }

    .header-left {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        align-items: center;
        width: 100%;
    }

    .header-back {
        justify-self: start;
    }

    .header-title {
        justify-self: center;
        text-align: center;
    }

    .header-clock {
        justify-self: end;
        text-align: right;
    }

    .header-title h3 {
        margin: 0;
    }

    .dashboard-wrap {
        display: flex;
        flex-direction: column;
        height: 100vh;
        box-sizing: border-box;
        overflow: hidden;
    }

    .dashboard-header {
        flex: 0 0 auto;
    }

    .qp-body {
        flex: 1 1 auto;
        min-height: 0;
        overflow: hidden;
    }

    .qp-col {
        display: flex;
        flex-direction: column;
        height: 100%;
        min-height: 0;
    }

    .qp-col-header {
        flex: 0 0 auto;
    }

    .qp-list {
        flex: 1 1 auto;
        min-height: 0;
        overflow-x: hidden;
        -webkit-overflow-scrolling: touch;
        padding-right: 6px;
    }

    .qp-list-left { overflow-y: hidden; }
    .qp-list-right { overflow-y: auto; }

    .qp-list::-webkit-scrollbar { width: 4px; }
    .qp-list::-webkit-scrollbar-track { background: transparent; }
    .qp-list::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.25);
        border-radius: 4px;
    }

    .qp-grid {
        display: grid;
        grid-template-columns: 2fr 1.5fr 1fr 1fr 1.5fr; 
        gap: 0.5rem;
        width: 100%;
        margin-bottom: 0.5rem;
    }

    .bg-card, .bg-card-line, .bg-card-rft, .bg-card-wft {
        min-height: 50px; 
        padding: 0.25rem;
        font-size: 16px; 
        font-weight: 700;
        border-radius: 5px;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
    }

    .bg-card { background-color: #292F40; color: white; border: 1px solid #D0D0D0; }
    .bg-card-line { background-color: #292F40; color: white; border: none; }
    .bg-card-rft { background-color: #1A3B45; border: 1px solid #4CF299; color: #4CF299; }
    .bg-card-wft { background-color: #23304E; border: 1px solid #3F5588; color: #4C8CF2; }

    .number-circle {
        width: 35px;
        height: 35px;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-size: 14px;
        font-weight: 600;
    }

    .footer-title {
        flex: 0 0 auto;
        padding-top: 0.75rem;
    }

    @media (min-width: 992px) and (max-width: 1400px) {
        #topDefectList { max-height: 83px; }
        .stat-card { height: 120px !important; min-height: 120px !important; }
        .stat-card img { width: 24px !important; height: 24px !important; margin-bottom: .35rem !important; }
        .stat-card h6 { font-size: 13px; margin-bottom: .25rem !important; }
        .stat-card h2 { font-size: 15px; }
        .stat-card small { font-size: 10px; }
        .defect-list { font-size: 10px; }
        .text-title { font-size: 12px; }

        .bg-card, .bg-card-line, .bg-card-rft, .bg-card-wft {
            min-height: 40px;
            font-size: 13px;
        }
        .number-circle {
            width: 25px; height: 25px; font-size: 11px;
        }
    }

    @media (max-width: 991px) {
        .dashboard-wrap {
            height: auto !important; 
            min-height: 100vh;
            overflow-y: auto; 
        }
        .qp-body, .qp-list, .qp-list-left {
            overflow: visible !important;
            height: auto;
        }
        .bg-card, .bg-card-line, .bg-card-rft, .bg-card-wft {
            min-height: 40px;
            font-size: 10px;
            padding: 2px;
        }
        .number-circle {
            width: 20px; height: 20px; font-size: 9px;
        }
        .qp-grid {
            gap: 0.25rem;
        }
        #topDefectList { max-height: 70px; }
        .defect-header { flex-direction: column; gap: 0.5rem !important; }
        .header-title h3 { font-size: 18px; }
    }
</style>
@endsection

@section('content')
<div class="container-fluid dashboard-wrap">
    <div class="dashboard-header">
        <div class="header-left pb-2">
            <div class="header-item header-back">
                <button
                    type="button"
                    onclick="history.back()"
                    class="back-btn border-0 bg-transparent text-white p-0 d-flex align-items-center gap-3">
                    <img src="{{ _asset('svg/inlinePerformance/back.svg') }}"
                        width="15"
                        height="15"
                        alt="Back">
                    <span>Back</span>
                </button>
            </div>

            <div class="header-item header-title text-white">
                <h3 class="mb-0">Quality Performance (Endline)</h3>
            </div>

            <div class="header-item header-clock text-white">
                <span id="liveClock">08.00 WIB</span>
            </div>
        </div>
    </div>

    @php
        $qpData = collect($data)->values();
    @endphp

    <div class="qp-body d-flex flex-wrap row mx-0 w-100">
        <div class="col-12 col-lg-6 qp-col px-1 px-lg-3 mb-3 mb-lg-0">
            <div class="qp-grid p-2 p-lg-4 qp-col-header">
                <span class="bg-card rounded-3 d-flex justify-content-center align-items-center">Location</span>
                <span class="bg-card rounded-3 d-flex justify-content-center align-items-center text-center">INSPECT QTY</span>
                <span class="bg-card-rft rounded-3 d-flex justify-content-center align-items-center">RFT %</span>
                <span class="bg-card-wft rounded-3 d-flex justify-content-center align-items-center">WFT %</span>
                <span class="bg-card rounded-3 d-flex justify-content-center align-items-center">TLS</span>
            </div>

            <div class="qp-list qp-list-left pb-4 pb-lg-0" id="qpListLeft"></div>
        </div>

        <div class="col-12 col-lg-6 qp-col px-3 d-none d-lg-flex">
            <div class="qp-grid p-4 qp-col-header">
                <span class="bg-card rounded-3 d-flex justify-content-center align-items-center">Location</span>
                <span class="bg-card rounded-3 d-flex justify-content-center align-items-center text-center">INSPECT QTY</span>
                <span class="bg-card-rft rounded-3 d-flex justify-content-center align-items-center">RFT %</span>
                <span class="bg-card-wft rounded-3 d-flex justify-content-center align-items-center">WFT %</span>
                <span class="bg-card rounded-3 d-flex justify-content-center align-items-center">TLS</span>
            </div>

            <div class="qp-list qp-list-right" id="qpListRight"></div>
        </div>
    </div>

    <script type="application/json" id="qpRowData">{!! $qpData->map(fn ($d) => ['namagroup' => $d->namagroup])->toJson() !!}</script>

    <div class="footer-title text-white d-flex flex-wrap justify-content-between align-items-center gap-2 flex-shrink-0">
        <div class="d-flex align-items-center gap-2">
            <img src="{{ _asset('svg/inlinePerformance/time.svg') }}" class="img-foot" alt="">
            Data Update
            <span id="liveClock2"></span>
        </div>
        <div class="d-flex gap-3">
            <span>LINE OFF = Worktime not setting yet</span>
            <span>MPS OFF = MPS not setting yet</span>
        </div>
    </div>
</div>

@section('extrascript')
<script>
    $(document).ready(function () {
        let savedTime = localStorage.getItem('lastDataUpdate');
        if (savedTime) {
            $('#liveClock2').html(savedTime);
        }
    });

    function qpAdjustDashboardHeight() {
        var wrap = document.querySelector('.dashboard-wrap');
        if (!wrap) return;

        if (window.innerWidth < 992) {
            wrap.style.height = 'auto';
            return;
        }

        var top = wrap.getBoundingClientRect().top;
        var available = window.innerHeight - top;
        wrap.style.height = Math.max(available - 8, 300) + 'px';
    }

    function qpEscapeHtml(str) {
        return String(str).replace(/[&<>"']/g, function (c) {
            return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
        });
    }

    function qpBuildRowHtml(name) {
        return '' +
            '<div class="qp-grid">' +
                '<span class="bg-card-line rounded-3">' + name + '</span>' +
                '<span class="bg-card-line rounded-3">20</span>' +
                '<span class="bg-card-line rounded-3" style="color:#4CF299;">98.21</span>' +
                '<span class="bg-card-line rounded-3" style="color:#4C8CF2;">1.79</span>' +
                '<div class="bg-card-line rounded-3 gap-1 gap-lg-2">' +
                    '<span class="number-circle" style="background-color: #BD3F3F3D;"></span>' +
                    '<span class="number-circle" style="background-color: #F9A51D40;"></span>' +
                    '<span class="number-circle" style="background-color: #1D9A5C;"></span>' +
                    '<span class="number-circle" style="background-color: #1744B740;"></span>' +
                '</div>' +
            '</div>';
    }

    function qpRenderRows() {
        var dataEl   = document.getElementById('qpRowData');
        var leftList = document.getElementById('qpListLeft');
        var rightList = document.getElementById('qpListRight');
        if (!dataEl || !leftList || !rightList) return;

        var rows = [];
        try {
            rows = JSON.parse(dataEl.textContent || '[]');
        } catch (e) {
            rows = [];
        }

        leftList.innerHTML = '';
        rightList.innerHTML = '';

        var isDesktop = window.innerWidth >= 992;

        if (isDesktop) {
            var approxRowHeight = (window.innerWidth <= 1400) ? 45 : 58;
            var availableHeight = leftList.clientHeight;
            var capacity = Math.max(1, Math.floor(availableHeight / approxRowHeight));

            var leftRows  = rows.slice(0, capacity);
            var rightRows = rows.slice(capacity);

            leftList.innerHTML  = leftRows.map(function (r) { return qpBuildRowHtml(qpEscapeHtml(r.namagroup)); }).join('');
            rightList.innerHTML = rightRows.map(function (r) { return qpBuildRowHtml(qpEscapeHtml(r.namagroup)); }).join('');
        } else {
            leftList.innerHTML = rows.map(function (r) { return qpBuildRowHtml(qpEscapeHtml(r.namagroup)); }).join('');
        }
    }

    function qpRefreshLayout() {
        qpAdjustDashboardHeight();
        requestAnimationFrame(qpRenderRows);
    }

    window.addEventListener('load', qpRefreshLayout);
    window.addEventListener('resize', qpRefreshLayout);
    document.addEventListener('DOMContentLoaded', qpRefreshLayout);
</script>
@endsection

@include('layouts.loading')
@endsection
