@extends('layouts.masternonavbar')

@section('extracss')
    <link rel="stylesheet" href="https://cdn.datatables.net/buttons/2.4.2/css/buttons.dataTables.min.css">

    <style>
        html, body {
            margin: 0;
            background-color: #242E3E;
        }

        body {
            padding: 1rem;
        }

        .dashboard-wrap {
            width: 100%;
        }

        .dashboard-header {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
        }

        .header-left {
            display: flex;
            align-items: center;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 16px;
            color: #ffffff;
            font-size: 1.1rem;
            font-weight: 600;
        }

        .back-btn {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        /* Text Color */
        .text-yellow { color: #F2C94C; }
        .text-red    { color: #F24C4C; }
        .text-blue   { color: #4C8CF2; }
        .text-green  { color: #4CF299; }

        /* Card Box */
        .color-box-yellow {
            background-color: #363633;
            border: 1px solid #F2C94C;
            border-radius: 5px;
            color: #F2C94C;
        }
        .color-box-red {
            background-color: #352837;
            border: 1px solid #F24C4C;
            border-radius: 5px;
        }
        .color-box-blue {
            background-color: #23304E;
            border: 1px solid #3F5588;
            border-radius: 5px;
        }
        .color-box-green {
            background-color: #1A3B45;
            border: 1px solid #4CF299;
            border-radius: 5px;
        }

        .metrics-wrap {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 0.75rem;
            margin-top: 0.75rem;
        }

        .location-box,
        .metric-card {
            min-height: 130px;
            padding: 1rem;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            gap: 0.5rem;
            font-size: 25px;
        }

        .metric-card .metric-value {
            display: flex;
            align-items: baseline;
            justify-content: start;
            gap: 0.25rem;
        }

        .metric-icon {
            flex-shrink: 0;
            width: 36px;
            height: 36px;
        }

        .metric-card h6 {
            font-size: 0.75rem;
            margin: 0;
        }

        .metric-card h2 {
            font-size: 1.25rem;
            margin: 0;
        }

        .datatable-wrap {
            display: flex;
            flex-direction: column;
            min-height: 0;
            min-width: 0;
            margin-top: 1rem;
        }

        .datatable-wrap .col-md-12 {
            flex: 1 1 auto;
            display: flex;
            min-width: 0;
            overflow: hidden;
        }

        #datatable-container {
            flex: 1 1 auto;
            min-width: 0;
            width: 100%;
            overflow: hidden;
            transition: opacity 0.15s ease;
        }

        .table-scroll-wrapper {
            overflow-x: auto;
            overflow-y: hidden;
            max-width: 100%;
            -webkit-overflow-scrolling: touch;
            width: 100%;
            height: 380px;
        }

        .export-btn {
            position: relative;
        }

        .custom-tooltip {
            position: absolute;
            top: 100%;
            right: 0;
            left: auto;
            transform: translateY(4px);
            margin-top: 8px;
            background-color: #000000;
            color: white;
            padding: 6px 18px;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 500;
            white-space: nowrap;
            opacity: 0;
            visibility: hidden;
            transition: all 0.2s ease;
            z-index: 10;
        }

        .custom-tooltip::after {
            content: "";
            position: absolute;
            bottom: 100%;
            right: 12px;
            border-width: 5px;
            border-style: solid;
            border-color: transparent transparent #000000 transparent;
        }

        .export-btn:hover .custom-tooltip {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        /* Select2 (Location Filter) */
        .select2-container {
            width: 100% !important;
        }

        .select2-container--default .select2-selection--single {
            background: #372E39;
            border: none;
            display: flex;
            align-items: center;
        }

        .select2-container--default .select2-selection--single .select2-selection__rendered {
            color: white;
            line-height: 38px;
            padding-left: 0.75rem;
        }

        .select2-container--default .select2-selection--single .select2-selection__arrow {
            height: 38px;
        }

        .select2-container--default .select2-selection--single .select2-selection__placeholder {
            color: #ffffff !important;
        }

        .select2-container--default .select2-results__option--highlighted[aria-selected] {
            background-color: transparent !important;
            color: white !important;
        }

        .select2-container--default .select2-results__option[aria-selected="true"] {
            background-color: transparent !important;
            color: white !important;
        }

        .select2-dropdown {
            background: #372E39;
            color: white;
        }

        .select2-results__option {
            color: white;
        }

        .select2-search__field {
            background: #372E39;
            color: white;
            border: none;
        }

        .round-status-table {
            --bs-table-bg: transparent;
            --bs-table-color: #ffffff;
            --bs-table-striped-bg: transparent;
            --bs-table-border-color: #576376;
            color: #ffffff;
        }

        .round-status-table th,
        .round-status-table td {
            vertical-align: middle;
            background-color: transparent;
            border-color: #576376;
            padding: 6px;
        }

        .round-status-table .round-label {
            font-weight: bold;
            padding: 1px;
        }

        .round-status-table .btn-detail {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            min-width: 64px;
            height: 100%;
            margin: 0 auto;
            cursor: default;
        }

        .round-status-table .btn-detail img {
            width: 16px;
            height: 16px;
        }

        .status-none   { background-color: #219653; }
        .status-one    { background-color: #F24C4C; }
        .status-multi  { background-color: #F24C4C; }
        .status-cap    { background-color: #F9E21D; }

        /* Legend */
        .legend-box {
            width: 22px;
            height: 22px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .legend-text {
            font-size: 1rem;
        }

        .legend-wrap {
            row-gap: 10px;
        }

        #datalist {
            table-layout: auto;
            height: 100%;
        }

        #datalist th,
        #datalist td {
            min-width: 75px;
            white-space: nowrap;
            font-size: 0.8rem;
        }

        .footer-title {
            justify-content: center;
            text-align: center;
            gap: 10px;
        }

        .dt-buttons .btn {
            margin-bottom: 8px;
        }

        @media (min-width: 400px) {
            .metric-icon {
                width: 40px;
                height: 40px;
            }

            .location-text {
                font-size: 13px;
            }
        }

        @media (min-width: 576px) {
            .metric-icon {
                width: 48px;
                height: 48px;
            }
            .metric-card h6 {
                font-size: 0.85rem;
            }
            .metric-card h2 {
                font-size: 1.5rem;
            }
            .table-scroll-wrapper {
                height: 420px;
            }
            #datalist th:first-child,
            #datalist td:first-child {
                min-width: 110px;
            }
            .round-status-table .btn-detail img {
                width: 18px;
                height: 18px;
            }
        }

        @media (min-width: 768px) {
            .metrics-wrap {
                gap: 1rem;
            }

            .metric-icon {
                width: 56px;
                height: 56px;
            }
            .header-right {
                font-size: 1.25rem;
            }
            .footer-title {
                justify-content: space-between;
                text-align: left;
                padding-top: 30px;
            }
            .title-system {
                display: none;
            }
            .table-scroll-wrapper {
                height: 480px;
            }
            #datalist th,
            #datalist td {
                min-width: 90px;
                font-size: 0.85rem;
            }

            html, body {
                height: 100%;
            }
            body {
                overflow: hidden;
                display: flex;
                flex-direction: column;
            }
            .dashboard-wrap {
                height: 100%;
                display: flex;
                flex-direction: column;
            }
            .datatable-wrap {
                flex: 1 1 auto;
                min-height: 0;
                overflow: hidden;
            }
            .table-scroll-wrapper {
                height: 100%;
            }

            .location-text {
                font-size: medium;
            }
        }

        @media (min-width: 992px) {
            html, body {
                height: 100%;
            }
            body {
                overflow: hidden;
                display: flex;
                flex-direction: column;
            }
            .dashboard-wrap {
                height: 100%;
                display: flex;
                flex-direction: column;
            }
            .datatable-wrap {
                flex: 1 1 auto;
                min-height: 0;
                overflow: hidden;
            }
            .table-scroll-wrapper {
                height: 100%;
            }
            .metric-icon {
                width: 60px;
                height: 60px;
            }
            #datalist th:first-child,
            #datalist td:first-child {
                min-width: 130px;
            }

            .location-text {
                font-size: 23px;
            }

        }

        @media (max-width: 767.98px) {
            .metrics-wrap {
                grid-template-columns: 1fr;
            }
        }

        @media (min-width: 768px) and (max-width: 991.98px) {
            .metrics-wrap {
                grid-template-columns: repeat(4, 1fr);
            }

            .metrics-wrap .location-box {
                grid-column: 1 / -1;
            }
        }

        /* Export Preview Modal */
        #exportPreviewModal .modal-content {
            background-color: #242E3E;
            color: #ffffff;
        }

        #exportPreviewModal .modal-header,
        #exportPreviewModal .modal-footer {
            border-color: #576376;
        }

        #exportPreviewModal .btn-close {
            filter: invert(1);
        }

        #exportTable {
            color: #ffffff;
        }

        #exportTable th,
        #exportTable td {
            border-color: #576376;
            white-space: nowrap;
        }

        #exportTableHead th {
            background-color: #242E3E;
            color: white;
        }

        #exportTableBody td {
            background-color: #242E3E;
            color: white;
        }
    </style>
@endsection

@section('content')
    <div class="container-fluid dashboard-wrap">
        <!-- Header -->
        <div class="dashboard-header">
            <div class="header-left">
                <button
                    type="button"
                    onclick="history.back()"
                    class="back-btn border-0 bg-transparent text-white p-0">
                    <img src="{{ _asset('svg/inlinePerformance/back.svg') }}" width="15" height="15">
                    <span>Back</span>
                </button>
            </div>

            <div class="header-right">
                <span id="liveClock"></span>
                <button type="button"
                    class="export-btn bg-transparent border-0 p-0 d-flex align-items-center"
                    data-bs-toggle="modal" data-bs-target="#exportPreviewModal">
                    <img src="{{ _asset('svg/inlinePerformance/export.svg') }}" style="width:27px; height:27px;">
                    <span class="custom-tooltip">Export</span>
                </button>
            </div>
        </div>

        <div class="metrics-wrap">
            <div class="location-box d-flex flex-column color-box-red">
                <h4 class="fw-bold text-red mb-0">LOCATION</h4>
                <span class="location-text fw-bold mb-0" style="color: white;">{{$namaline}}</span>
            </div>

            <!-- QTY INSPECT -->
            <div class="color-box-yellow metric-card ">
                <img src="{{ _asset('svg/inlinePerformance/qty-inspect.svg') }}" class="metric-icon img-fluid">
                <div class="justify-content-start">
                    <h6 class="fw-bold text-yellow">INSPECT QTY</h6>
                    <div class="metric-value text-white justify-content-start">
                        <h2 class="fw-bold" id="tot_inspect">{{ $tot_inspect ?? 0 }}</h2>
                        <small>pcs</small>
                    </div>
                </div>
            </div>

            <!-- QTY DEFECT -->
            <div class="color-box-red metric-card">
                <img src="{{ _asset('svg/inlinePerformance/qty-defect.svg') }}" class="metric-icon img-fluid">
                <div class="justify-content-start">
                    <h6 class="fw-bold text-red">DEFECT QTY</h6 >
                    <div class="metric-value text-white justify-content-start">
                        <h2 class="fw-bold" id="tot_defect">{{ $tot_defect ?? 0 }}</h2>
                        <small>pcs</small>
                    </div>
                </div>
            </div>

            <!-- RFT -->
            <div class="color-box-green metric-card">
                <img src="{{ _asset('svg/inlinePerformance/rft.svg') }}" class="metric-icon img-fluid">
                <div class="justify-content-start">
                    <h6 class="fw-bold text-green text-start">RFT</h6>
                    <div class="metric-value text-white justify-content-start">
                        <h2 class="fw-bold"><span id="rft">{{ $rft ?? '0.00' }}%</span></h2>
                    </div>
                </div>
            </div>

            <!-- WFT -->
            <div class="color-box-blue metric-card">
                <img src="{{ _asset('svg/inlinePerformance/wft.svg') }}" class="metric-icon img-fluid">
                <div>
                    <h6 class="fw-bold text-blue text-start">WFT</h6>
                    <div class="metric-value text-white">
                        <h2 class="fw-bold"><span id="wft">{{ $wft ?? '0.00' }}%</span></h2>
                    </div>
                </div>
            </div>
        </div>

        <!-- Round Status Table -->
        <div class="rounded text-white datatable-wrap">
            <div class="col-md-12 m-0">
                <div id="datatable-container" class="w-100">
                    @include('monitoring::inlineperformance.inlineLts.datatable', [
                        'operators' => $operators ?? [],
                        'rounds'    => $rounds ?? [],
                    ])
                </div>
            </div>
        </div>

        <!-- Legend -->
        <div class="d-flex justify-content-center mt-2">
            <div class="legend-wrap d-flex flex-wrap text-white py-1 gap-3 fs-6">
                <div class="d-flex gap-3 align-items-center">
                    <div class="legend-box" style="background-color: #219653;"></div>
                    <p class="legend-text mb-0">No Defect Data</p>
                </div>

                <div class="d-flex gap-3 align-items-center">
                    <div class="legend-box" style="background-color: #F24C4C;"></div>
                    <p class="legend-text mb-0">Found One Defect Data</p>
                </div>

                <div class="d-flex gap-3 align-items-center">
                    <div class="legend-box" style="background-color: #F24C4C;">
                        <img class="legend-icon" style="width: 12px; height:12px" src="{{ _asset('svg/inlinePerformance/defect2.svg') }}">
                    </div>
                    <p class="legend-text mb-0">Found Two or More Defect Data</p>
                </div>

                <div class="d-flex gap-3 align-items-center">
                    <div class="legend-box" style="background-color: #F9E21D;">
                        <img class="legend-icon" style="width: 12px; height:12px" src="{{ _asset('svg/inlinePerformance/pencil.svg') }}">
                    </div>
                    <p class="legend-text mb-0">CAP Already Filled In</p>
                </div>
            </div>
        </div>

        <!-- Footer -->
         <div class="footer-title mt-2 text-white d-flex flex-wrap justify-content-between align-items-center gap-2">
            <div class="d-flex align-items-center gap-2">
                <img src="{{ _asset('svg/inlinePerformance/time.svg') }}" style="width: 18px; height: 18px;">
                Data Update
                <span id="liveClock2"></span>
            </div>
        </div>
    </div>

    <!-- Export Preview Modal -->
    <div class="modal fade" id="exportPreviewModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-xl modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Export Data</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div id="exportLoading" class="text-center text-white py-4">
                        Loading...
                    </div>
                    <div class="table-responsive table-scroll-wrapper">
                        <table id="exportTable" class="table table-bordered table-sm w-100 d-none">
                            <thead id="exportTableHead"></thead>
                            <tbody id="exportTableBody"></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection

@section('extrascript')
    <script>
        $(document).ready(function () {

           $(document).ready(function () {
                let savedTime = localStorage.getItem('lastDataUpdate');
                if (savedTime) {
                    $('#liveClock2').html(savedTime);
                }
            });

            let currentLine      = @json($kodegroup ?? null);
            let currentLineLabel = @json($namaline ?? null);
            let currentDate       = @json($date);
            let skipInitialChange = true;
            let exportDT          = null;

            const formattedDate = new Date(currentDate).toLocaleDateString('en-GB', {
                day: 'numeric',
                month: 'long',
                year: 'numeric'
            });

           function formatDate(date) {
                const d = new Date(date);

                const months = [
                    'January', 'February', 'March', 'April', 'May', 'June',
                    'July', 'August', 'September', 'October', 'November', 'December'
                ];

                const day = d.getDate();
                const monthName = months[d.getMonth()];
                const year = d.getFullYear();

                const hours = String(d.getHours()).padStart(2, '0');
                const minutes = String(d.getMinutes()).padStart(2, '0');
                const seconds = String(d.getSeconds()).padStart(2, '0');

                return `${day} ${monthName} ${year}`;
            }

            function updateLiveClock() {
                const now = new Date();

                const formatted = formatDate(now);

                let currentDate = @json($date);

                const dateTime = currentDate ? formatDate(currentDate) : formatted;

                $('#liveClock').text(dateTime);
            }
            updateLiveClock();

            function updateClock() {
                const now = new Date();
                const hour = String(now.getHours()).padStart(2, '0');
                const minute = String(now.getMinutes()).padStart(2, '0');
                const second = String(now.getSeconds()).padStart(2, '0');

                const formatted = `${day} ${month[now.getMonth()]} ${year} - ${hour} : ${minute} : ${second}`;

                const addDate = `${formattedDate} - ${hour} : ${minute} : ${second}`;
                document.getElementById('liveClock').innerHTML = addDate;
            }

            $('#line').select2({
                placeholder: 'Select Line',
                width: '100%',
                ajax: {
                    url: "{{ route('monitoring_inlineperformanceselectline') }}",
                    type: "GET",
                    dataType: "json",
                    delay: 500,
                    data: function (params) {
                        return {
                            q: params.term,
                            page: params.page || 1
                        };
                    },
                    processResults: function (data) {
                        return {
                            results: $.map(data.data, function (item) {
                                return {
                                    id: item.kodegroup,
                                    text: item.namabagian + ' - ' + item.namagroup
                                };
                            }),
                            pagination: {
                                more: data.to < data.total
                            }
                        };
                    },
                    cache: true
                }
            });

            if (currentLine) {
                const preOption = new Option(currentLineLabel, currentLine, true, true);
                $('#line').append(preOption).trigger('change');
            }

            function fetchInlineLts() {
                if (!currentLine) return;

                $('#datatable-container').css('opacity', 0.5);

                $.ajax({
                    url: "{{ route('monitoring_inlineperformance_filterInlineLts') }}",
                    type: "GET",
                    data: { line: currentLine, date: currentDate },
                    success: function (res) {
                        $('#tot_inspect').text(res.tot_inspect);
                        $('#tot_defect').text(res.tot_defect);
                        $('#rft').text(res.rft);
                        $('#wft').text(res.wft);
                        $('#datatable-container').html(res.html);
                    },
                    error: function (xhr) {
                        console.log(xhr.responseJSON ? xhr.responseJSON.message : 'Request failed');
                    },
                    complete: function () {
                        $('#datatable-container').css('opacity', 1);
                    }
                });
            }

            $('#line').on('change', function () {
                if (skipInitialChange) {
                    skipInitialChange = false;
                    return;
                }
                currentLine = $(this).val();
                fetchInlineLts();
            });

            function resetExportModal() {
                if (exportDT) {
                    exportDT.destroy();
                    exportDT = null;
                }
                $('#exportTableHead').empty();
                $('#exportTableBody').empty();
                $('#exportTable').addClass('d-none');
                $('#exportLoading').removeClass('d-none').text('Loading...');
            }

            function loadExportPreview() {
                resetExportModal();

                $.ajax({
                    url: "{{ route('monitoring_inlineperformance_exportData') }}",
                    type: "GET",
                    data: { line: currentLine, date: currentDate },
                    dataType: "json",
                    success: function (res) {
                        if (!res || !res.headers || !res.rows) {
                            $('#exportLoading').text('Data export is not available.');
                            return;
                        }

                        let theadHtml = '<tr>' + res.headers.map(function (h) {
                            return '<th>' + h + '</th>';
                        }).join('') + '</tr>';
                        $('#exportTableHead').html(theadHtml);

                        let tbodyHtml = res.rows.map(function (row) {
                            return '<tr>' + row.map(function (cell) {
                                return '<td>' + (cell ?? '') + '</td>';
                            }).join('') + '</tr>';
                        }).join('');
                        $('#exportTableBody').html(tbodyHtml);

                        $('#exportLoading').addClass('d-none');
                        $('#exportTable').removeClass('d-none');

                        exportDT = $('#exportTable').DataTable({
                            scrollX: true,
                            paging: false,
                            searching: false,
                            lengthChange: true,
                            dom: 'Bfrtip',
                            buttons: [
                                {
                                    extend: 'excelHtml5',
                                    text: 'Download Excel',
                                    className: 'btn btn-success btn-sm',
                                    title: 'Inline LTS' + (currentLine || '') + '_' + (currentDate || '')
                                }
                            ]
                        });
                    },
                    error: function (xhr) {
                        $('#exportLoading').text('Gagal memuat data export.');
                        console.log(xhr.responseJSON ? xhr.responseJSON.message : 'Request failed');
                    }
                });
            }

            $('.export-btn').on('click', function () {
                if (!currentLine) {
                    alert('Select the line first before exporting.');
                    return false;
                }
            });

            $('#exportPreviewModal').on('show.bs.modal', function (e) {
                if (!currentLine) {
                    e.preventDefault();
                    alert('Select the line first before exporting.');
                    return;
                }
                loadExportPreview();
            });

            $('#exportPreviewModal').on('hidden.bs.modal', function () {
                resetExportModal();
            });

        });
    </script>
@endsection
